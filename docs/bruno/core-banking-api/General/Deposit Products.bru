meta {
  name: Deposit Products
  type: http
  seq: 5
}

get {
  url: {{base_url}}/products/deposits
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Deposit Products

  Menampilkan daftar produk deposito beserta suku bunga per tenor.

  **Security**: AUTH (Bearer Token)
}
