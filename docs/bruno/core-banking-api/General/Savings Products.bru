meta {
  name: Savings Products
  type: http
  seq: 4
}

get {
  url: {{base_url}}/products/savings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Savings Products

  Menampilkan daftar produk tabungan yang tersedia.

  **Security**: AUTH (Bearer Token)
}
