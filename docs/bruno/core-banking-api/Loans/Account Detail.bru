meta {
  name: Account Detail
  type: http
  seq: 2
}

get {
  url: {{base_url}}/loans/{{loan_account_number}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Loan Account Detail

  Menampilkan detail rekening pinjaman tertentu.

  **Security**: AUTH (Bearer Token)
}
