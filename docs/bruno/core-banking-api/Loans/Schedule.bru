meta {
  name: Schedule
  type: http
  seq: 3
}

get {
  url: {{base_url}}/loans/{{loan_account_number}}/schedule
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Loan Schedule

  Menampilkan jadwal angsuran pinjaman (amortization schedule).

  **Security**: AUTH (Bearer Token)
}
