meta {
  name: Payment History
  type: http
  seq: 5
}

get {
  url: {{base_url}}/loans/{{loan_account_number}}/payments
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Payment History

  Menampilkan riwayat pembayaran angsuran pinjaman.

  **Security**: AUTH (Bearer Token)
}
