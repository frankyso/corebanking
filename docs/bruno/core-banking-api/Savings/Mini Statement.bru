meta {
  name: Mini Statement
  type: http
  seq: 5
}

get {
  url: {{base_url}}/savings/{{savings_account_number}}/mini-statement
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Mini Statement

  Menampilkan 10 transaksi terakhir pada rekening tabungan.

  **Security**: AUTH (Bearer Token)
}
