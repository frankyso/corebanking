meta {
  name: Account Detail
  type: http
  seq: 2
}

get {
  url: {{base_url}}/savings/{{savings_account_number}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Savings Account Detail

  Menampilkan detail rekening tabungan tertentu.

  **Security**: AUTH (Bearer Token)
}
