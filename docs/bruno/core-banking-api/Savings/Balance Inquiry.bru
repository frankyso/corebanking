meta {
  name: Balance Inquiry
  type: http
  seq: 3
}

get {
  url: {{base_url}}/savings/{{savings_account_number}}/balance
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Balance Inquiry

  Cek saldo rekening tabungan (balance, hold_amount, available_balance).

  **Security**: AUTH (Bearer Token)
}
