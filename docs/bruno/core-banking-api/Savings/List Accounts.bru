meta {
  name: List Accounts
  type: http
  seq: 1
}

get {
  url: {{base_url}}/savings
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # List Savings Accounts

  Menampilkan semua rekening tabungan milik nasabah.

  **Security**: AUTH (Bearer Token)
}
