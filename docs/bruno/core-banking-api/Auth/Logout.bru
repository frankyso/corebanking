meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{base_url}}/auth/logout
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Logout

  Menghapus token sesi saat ini.

  **Security**: AUTH (Bearer Token)

  **Response** (200):
  ```json
  {
    "message": "Logout berhasil."
  }
  ```
}
