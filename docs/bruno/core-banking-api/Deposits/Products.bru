meta {
  name: Products
  type: http
  seq: 4
}

get {
  url: {{base_url}}/deposits/products
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Deposit Products

  Menampilkan daftar produk deposito beserta suku bunga per tenor.

  **Security**: AUTH (Bearer Token)
}
