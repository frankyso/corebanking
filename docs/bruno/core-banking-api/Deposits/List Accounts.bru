meta {
  name: List Accounts
  type: http
  seq: 1
}

get {
  url: {{base_url}}/deposits
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # List Deposit Accounts

  Menampilkan semua rekening deposito milik nasabah.

  **Security**: AUTH (Bearer Token)
}
