meta {
  name: Remove Favorite
  type: http
  seq: 8
}

delete {
  url: {{base_url}}/transfers/favorites/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # Remove Transfer Favorite

  Menghapus rekening dari daftar favorit transfer.

  **Security**: AUTH (Bearer Token)

  **Response** (200):
  ```json
  {
    "message": "Favorit berhasil dihapus."
  }
  ```
}
