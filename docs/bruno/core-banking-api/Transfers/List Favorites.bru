meta {
  name: List Favorites
  type: http
  seq: 6
}

get {
  url: {{base_url}}/transfers/favorites
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Accept: application/json
  X-Device-Id: {{device_id}}
}

docs {
  # List Transfer Favorites

  Menampilkan daftar rekening favorit untuk transfer.

  **Security**: AUTH (Bearer Token)
}
